if(window.SimpleSlide) {
    new SimpleSlide({
        slide: "quote",   // nome do atributo data-side="principal".
        time: 5000   // tempo de trasição dos slides.
    });
    
    new SimpleSlide({
        slide: "portfolio",   // nome do atributo data-side="principal".
        time: 5000,   // tempo de trasição dos slides.
        nav: true,
    });
}